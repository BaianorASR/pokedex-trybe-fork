import styled from 'styled-components';

export const ScPlaylistList = styled.div`
  display: flex;
  overflow-x: hidden;
  overflow-y: hidden;
  gap: 8px;
`;

export const ScPlaylistImage = styled.img`
  width: 125px;
`;
